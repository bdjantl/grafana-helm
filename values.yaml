# values.yaml (example for customization)
grafana:
  service:
    type: ClusterIP # or LoadBalancer if supported by your local setup
  persistence:
    enabled: false
    storageClassName: standard # or an appropriate storage class for your local cluster
